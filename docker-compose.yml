services:
  outbound-monitor:
    build: .
    container_name: outbound-traffic-monitor
    env_file:
      - .env
    volumes:
      - outbound_data:/app/data
    restart: unless-stopped
    network_mode: host
    privileged: true 

volumes:
  outbound_data:
