services:
  outbound-monitor:
    build: .
    container_name: outbound-traffic-monitor
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - outbound_data:/app/data
    restart: unless-stopped
    network_mode: host
    privileged: true 

volumes:
  outbound_data:
